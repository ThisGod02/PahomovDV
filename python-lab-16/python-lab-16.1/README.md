# Lab 16.1 - GigaChat Assistant Demo

This practical part supports two modes:

- `mock` - works without credentials and is safe for local demo
- `real` - uses the `gigachat` SDK if credentials are present in `.env`

Quick start:

```bash
pip install -r requirements.txt
python demo_tasks.py
```
