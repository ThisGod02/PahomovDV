# **Отчёт по лабораторной работе №12. Часть 1**
**Тема:** Нативная Android-разработка с Jetpack Compose

## Сведения о студенте
**Дата:** 2026-03-26
**Студент:** Пахомов Давид Вадимович

---

## 1. Цель работы
Получить практические навыки создания нативного Android-приложения на Kotlin с использованием современного декларативного подхода Jetpack Compose, архитектурных компонентов Jetpack (ViewModel, StateFlow) и локальной базы данных Room.

## 2. Структура проекта
Проект реализован по архитектуре MVVM (Model-View-ViewModel):
- `data/` — Модели данных (Room Entity), DAO и репозиторий.
- `ui/` — Компоненты Jetpack Compose (Screen, Item) и ViewModel.
- `MainActivity.kt` — Точка входа и настройка навигации.

## 3. Реализация TODO-задач

### 3.1. Модель данных (Note.kt)
Добавлено поле `createdAt` для хранения времени создания заметки.

```kotlin
@Entity(tableName = "notes")
data class Note(
    @PrimaryKey(autoGenerate = true)
    val id: Int = 0,
    val title: String,
    val content: String,
    val createdAt: Long = System.currentTimeMillis()
)
```

### 3.2. Удаление заметок (NoteDao.kt, NotesViewModel.kt)
Реализован метод удаления в DAO и соответствующая логика во ViewModel.

```kotlin
// NoteDao.kt
@Delete
suspend fun deleteNote(note: Note)

// NotesViewModel.kt
fun deleteNote(note: Note) {
    viewModelScope.launch {
        repository.deleteNote(note)
    }
}
```

### 3.3. Отображение даты (NotesScreen.kt)
Добавлена функция форматирования даты и ее вывод в карточке заметки.

```kotlin
fun formatDate(timestamp: Long): String {
    val dateFormat = SimpleDateFormat("dd MMM yyyy, HH:mm", Locale.getDefault())
    return dateFormat.format(timestamp)
}
```

## 4. Ответы на вопросы

**1. В чем преимущество использования Flow и StateFlow перед обычными списками?**
Flow позволяет реализовать реактивное программирование: UI автоматически обновляется при изменении данных в БД. StateFlow же хранит последнее состояние, что удобно для привязки к Compose.

**2. Почему ViewModel не уничтожается при повороте экрана и как это влияет на UX?**
ViewModel переживает смену конфигурации благодаря жизненному циклу Android. Это позволяет сохранять загруженные данные и текущее состояние ввода, предотвращая потерю прогресса пользователя.

**3. Какие преимущества дает использование Room по сравнению с прямым использованием SQLite?**
Room предоставляет проверку SQL-запросов на этапе компиляции, избавляет от написания шаблонного кода (Boilerplate) и обеспечивает легкую интеграцию с Flow/Coroutines.

## 5. Заключение
В ходе лабораторной работы было разработано полноценное Android-приложение "Менеджер заметок". Были освоены принципы работы с Jetpack Compose, навигацией, корутинами и базой данных Room. Приложение поддерживает создание, редактирование, просмотр и удаление заметок с сохранением данных.
