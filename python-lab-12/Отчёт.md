# Отчёт по лабораторной работе №12
**Тема:** Мобильная разработка: нативные, кроссплатформенные приложения и PWA  
**Студент:** Пахомов Давид Вадимович  
**Группа:** ПИН-Б-О-24-2  
**Дата:** 2026-03-26

---

## Содержание

1. [Часть 1: Нативная Android-разработка (Jetpack Compose)](#часть-1)
2. [Часть 2: Кроссплатформенная разработка (React Native)](#часть-2)
3. [Часть 3: Кроссплатформенная разработка (Flutter)](#часть-3)
4. [Часть 4: Kotlin Multiplatform Mobile (KMM)](#часть-4)
5. [Часть 5: Progressive Web Apps (PWA)](#часть-5)
6. [Сравнительная таблица технологий](#сравнение)
7. [Общий вывод](#вывод)

---

## Часть 1: Нативная Android-разработка с Jetpack Compose {#часть-1}

**Задание:** Приложение «Персональный менеджер заметок» (Notes App)  
**Стек:** Kotlin, Jetpack Compose, Room, ViewModel, StateFlow, Navigation

### Структура проекта

```
NotesApp/
├── data/
│   ├── Note.kt          — Room Entity
│   ├── NoteDao.kt       — DAO-интерфейс (CRUD)
│   ├── NoteDatabase.kt  — Singleton базы данных
│   └── NoteRepository.kt — Репозиторий
├── ui/
│   ├── NotesViewModel.kt      — ViewModel (CRUD-логика)
│   ├── NotesViewModelFactory.kt
│   ├── NotesScreen.kt         — Список заметок
│   └── AddEditNoteScreen.kt   — Экран создания/редактирования
└── MainActivity.kt    — Навигация
```

### Реализация задач (TODO)

**1. Добавление поля `createdAt` (Note.kt)**

```kotlin
@Entity(tableName = "notes")
data class Note(
    @PrimaryKey(autoGenerate = true)
    val id: Int = 0,
    val title: String,
    val content: String,
    val createdAt: Long = System.currentTimeMillis()
)
```

**2. Удаление заметок (NoteDao.kt + NotesViewModel.kt)**

```kotlin
// NoteDao.kt
@Delete
suspend fun deleteNote(note: Note)

// NotesViewModel.kt
fun deleteNote(note: Note) {
    viewModelScope.launch {
        repository.deleteNote(note)
    }
}
```

**3. Форматирование и отображение даты (NotesScreen.kt)**

```kotlin
fun formatDate(timestamp: Long): String {
    val dateFormat = SimpleDateFormat("dd MMM yyyy, HH:mm", Locale.getDefault())
    return dateFormat.format(timestamp)
}

// В NoteItem()
Text(
    text = formatDate(note.createdAt),
    style = MaterialTheme.typography.labelSmall,
    color = MaterialTheme.colorScheme.outline
)
```

**4. Навигация к редактированию (MainActivity.kt)**

```kotlin
composable("add_edit_note/{noteId}") { backStackEntry ->
    val noteId = backStackEntry.arguments?.getString("noteId")?.toInt() ?: 0
    val vm: NotesViewModel = viewModel(factory = NotesViewModelFactory(noteRepository))
    LaunchedEffect(noteId) { vm.loadNoteById(noteId) }
    val note by vm.currentNote.collectAsState()
    if (note != null) {
        AddEditNoteScreen(
            initialTitle = note!!.title,
            initialContent = note!!.content,
            onSaveClick = { title, content -> vm.updateNote(noteId, title, content) },
            onNavigateBack = { navController.popBackStack() }
        )
    }
}
```

### Ответы на вопросы

1. **Преимущества Flow и StateFlow:** `Flow` реализует реактивное программирование — UI обновляется автоматически при любом изменении данных в Room. `StateFlow` сохраняет последнее состояние и немедленно эмитирует его новым подписчикам, что идеально для привязки к Compose.
2. **ViewModel и повороты экрана:** `ViewModel` хранится в специальном `ViewModelStore`, привязанном к `Activity`, а не её жизненному циклу. При пересоздании Activity (например, при повороте) `ViewModel` *не уничтожается*, что сохраняет загруженные данные и не вызывает повторного обращения к БД.
3. **Room vs прямой SQLite:** Room проверяет корректность SQL-запросов *на этапе компиляции*, избавляет от шаблонного кода (`Cursor`, `ContentValues`) и обеспечивает нативную интеграцию с Coroutines и Flow.

---

## Часть 2: Кроссплатформенная разработка с React Native {#часть-2}

**Задание:** Приложение «Гео-заметки» (GeoNotes)  
**Стек:** React Native, Expo, TypeScript, Redux Toolkit, expo-sqlite, expo-location

### Структура проекта

```
GeoNotes/
├── src/
│   ├── types/index.ts       — Типы данных (GeoNote, AppState)
│   ├── utils/database.ts    — Работа с SQLite (CRUD)
│   ├── store/
│   │   ├── index.ts         — Конфигурация Redux Store
│   │   └── notesSlice.ts    — Редьюсер и Thunks
│   ├── hooks/reduxHooks.ts  — Типизированные хуки
│   └── screens/
│       ├── NotesListScreen.tsx
│       ├── AddNoteScreen.tsx
│       └── NoteDetailScreen.tsx
└── App.tsx
```

### Реализация задач (TODO)

**1. Типы данных (types/index.ts)**

```typescript
export interface GeoNote {
    id: string;
    title: string;
    content: string;
    latitude: number;
    longitude: number;
    address?: string;
    photoUri?: string;
    createdAt: number;
    updatedAt: number; // TODO: добавлено поле
}

export interface AppState { // TODO: создан интерфейс состояния
    notes: GeoNote[];
    currentLocation: Location | null;
    isLoading: boolean;
    error: string | null;
}
```

**2. Операции с БД (utils/database.ts)**

```typescript
export const addNote = (note: GeoNote): Promise<void> => {
    return new Promise((resolve, reject) => {
        db.transaction(tx => {
            tx.executeSql(
                'INSERT INTO notes (id, title, content, latitude, longitude, address, photoUri, createdAt) VALUES (?, ?, ?, ?, ?, ?, ?, ?)',
                [note.id, note.title, note.content, note.latitude, note.longitude,
                 note.address || null, note.photoUri || null, note.createdAt],
                () => resolve(),
                (_, error) => { reject(error); return false; }
            );
        });
    });
};
```

**3. Redux Thunks (store/notesSlice.ts)**

```typescript
export const saveNote = createAsyncThunk(
    'notes/saveNote',
    async (note: GeoNote) => {
        await database.addNote(note); // TODO: подключена реальная функция
        return note;
    }
);

export const removeNote = createAsyncThunk(
    'notes/removeNote',
    async (id: string) => {
        await database.deleteNote(id); // TODO: подключена реальная функция
        return id;
    }
);
```

### Ответы на вопросы

1. **React Native vs Flutter:** React Native использует нативные UIKit/Android View через «мост» (Bridge/JSI), что обеспечивает *нативный look&feel*. Flutter отрисовывает весь UI самостоятельно через движок Skia/Impeller — и выглядит *одинаково* на всех платформах.
2. **Роль Redux в мобильном приложении:** Redux централизует состояние, доступное на разных экранах (например, список заметок). Это устраняет *prop drilling* и упрощает отладку через Redux DevTools.

---

## Часть 3: Кроссплатформенная разработка с Flutter {#часть-3}

**Задание:** Приложение «Трекинг активности» (Activity Tracker)  
**Стек:** Dart, Flutter, Provider, sqflite, sensors_plus, pedometer

### Структура проекта

```
activity_tracker/lib/
├── models/
│   ├── activity.dart    — ActivityRecord, ActivityType, LatLng
│   └── user_stats.dart  — UserStats, DailyStats
├── services/
│   ├── database_service.dart — SQLite CRUD
│   └── sensor_service.dart   — Шагомер, GPS, Акселерометр
├── providers/
│   ├── activity_provider.dart — Управление состоянием
│   └── theme_provider.dart
└── widgets/
    └── activity_card.dart
```

### Реализация задач (TODO)

**1. Свойство `pace` и сериализация (models/activity.dart)**

```dart
// Расчет темпа (мин/км)
double get pace {
  if (distance == 0) return 0.0;
  return duration.inMinutes / distance;
}

// Сохранение в БД
Map<String, dynamic> toMap() {
  return {
    'id': id,
    'type': type.index,
    'startTime': startTime.millisecondsSinceEpoch,
    'endTime': endTime?.millisecondsSinceEpoch,
    'steps': steps,
    'distance': distance,
    'calories': calories,
    'route': route != null ? jsonEncode(route!.map((e) => e.toJson()).toList()) : null,
  };
}

// Загрузка из БД
factory ActivityRecord.fromMap(Map<String, dynamic> map) {
  return ActivityRecord(
    id: map['id'],
    type: ActivityType.values[map['type']],
    startTime: DateTime.fromMillisecondsSinceEpoch(map['startTime']),
    // ...
  );
}
```

**2. CRUD в базе данных (services/database_service.dart)**

```dart
Future<List<ActivityRecord>> getAllActivities() async {
  final db = await database;
  final maps = await db.query('activities', orderBy: 'startTime DESC');
  return List.generate(maps.length, (i) => ActivityRecord.fromMap(maps[i]));
}

Future<void> deleteActivity(String id) async {
  final db = await database;
  await db.delete('activities', where: 'id = ?', whereArgs: [id]);
}
```

### Ответы на вопросы

1. **Почему Dart?** Dart компилируется в нативный ARM-код (AOT) для релиза и использует JIT-компиляцию для разработки, обеспечивая поддержку ***Hot Reload***. Язык разработан Google специально для клиентских приложений.
2. **Provider vs setState:** `setState` перерисовывает *весь* виджет. `Provider` (обертка над `InheritedWidget`) уведомляет только тех потребителей, которым нужны изменённые данные — это экономит ресурсы.

---

## Часть 4: Kotlin Multiplatform Mobile (KMM) {#часть-4}

**Задание:** Приложение «Калькулятор расходов» (Expense Tracker)  
**Стек:** Kotlin Multiplatform, SQLDelight, Koin, Jetpack Compose (Android), SwiftUI (iOS)

### Структура проекта

```
ExpenseTracker/
├── shared/src/
│   ├── commonMain/kotlin/     — Общая бизнес-логика
│   │   ├── models/Expense.kt  — Модели данных
│   │   ├── db/Database.kt     — SQLDelight обёртка
│   │   └── viewmodels/ExpensesViewModel.kt
│   ├── androidMain/kotlin/    — Android-специфичный код
│   └── iosMain/kotlin/        — iOS-специфичный код
├── androidApp/                — Jetpack Compose UI
└── iosApp/                    — SwiftUI UI
```

### Реализация задач (TODO)

**1. Модель `CategoryStats` (models/Expense.kt)**

```kotlin
data class CategoryStats(
    val category: Category,
    val totalSpent: Double,
    val budget: Double?,
    val transactionCount: Int
)
```

**2. Расчет статистики в ViewModel (viewmodels/ExpensesViewModel.kt)**

```kotlin
fun calculateCategoryStats(): List<CategoryStats> {
    val s = state.value
    return s.categories.map { category ->
        val spent = s.expenses
            .filter { it.categoryId == category.id }
            .sumOf { it.amount }
        val budget = s.budgets.find { it.categoryId == category.id }?.amount
        CategoryStats(
            category = category,
            totalSpent = spent,
            budget = budget,
            transactionCount = s.expenses.count { it.categoryId == category.id }
        )
    }
}
```

**3. Механизм `expect/actual` (DatabaseDriverFactory)**

```kotlin
// commonMain — объявление
expect class DatabaseDriverFactory {
    fun createDriver(): SqlDriver
}

// androidMain — реализация
actual class DatabaseDriverFactory(private val context: Context) {
    actual fun createDriver(): SqlDriver =
        AndroidSqliteDriver(ExpenseDatabase.Schema, context, "expense.db")
}
```

### Ответы на вопросы

1. **KMM vs Flutter/React Native:** KMM разделяет *только логику* (код на Kotlin), полностью сохраняя нативный UI. Flutter и React Native имеют собственные UI-фреймворки, что жертвует долей нативности ради единого кода.
2. **`expect/actual`:** Это механизм условной компиляции. `expect` описывает *интерфейс* в общем коде, `actual` — конкретную реализацию для каждой платформы.

---

## Часть 5: Progressive Web Apps (PWA) {#часть-5}

**Задание:** Приложение «Новости PWA» (PWA News Aggregator)  
**Стек:** Node.js, Express, Vanilla JS, Web App Manifest, Service Workers, Web Push API

### Структура проекта

```
pwa-news/
├── server/
│   └── index.js       — Express-сервер, эндпоинты API, Push-уведомления
├── public/
│   ├── index.html     — Главная страница (SPA)
│   ├── manifest.json  — PWA Manifest
│   ├── sw.js          — Service Worker (кэширование)
│   ├── css/styles.css
│   └── js/
│       ├── app.js     — Логика приложения
│       └── pwa.js     — Регистрация SW, Install Prompt
└── .env
```

### Реализация задач (TODO)

**1. API категорий (server/index.js)**

```javascript
app.get('/api/categories', (req, res) => {
    res.json(['general', 'technology', 'science', 'sports', 'entertainment']);
});
```

**2. Регистрация Service Worker (public/js/pwa.js)**

```javascript
if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/sw.js')
        .then(reg => console.log('SW зарегистрирован:', reg.scope))
        .catch(err => console.error('SW ошибка:', err));
}

// Обработка Install Prompt
let deferredPrompt;
window.addEventListener('beforeinstallprompt', (e) => {
    e.preventDefault();
    deferredPrompt = e;
    document.getElementById('installBtn').classList.remove('hidden');
});

document.getElementById('installBtn')?.addEventListener('click', async () => {
    if (deferredPrompt) {
        deferredPrompt.prompt();
        const { outcome } = await deferredPrompt.userChoice;
        console.log('Install outcome:', outcome);
        deferredPrompt = null;
    }
});
```

**3. Service Worker с кэшированием (public/sw.js)**

```javascript
const CACHE_NAME = 'pwa-news-v1';
const STATIC_ASSETS = ['/', '/css/styles.css', '/js/app.js', '/js/pwa.js', '/manifest.json'];

self.addEventListener('install', (event) => {
    event.waitUntil(
        caches.open(CACHE_NAME).then(cache => cache.addAll(STATIC_ASSETS))
    );
});

self.addEventListener('fetch', (event) => {
    event.respondWith(
        caches.match(event.request).then(cached => cached || fetch(event.request))
    );
});
```

### Ответы на вопросы

1. **Service Worker:** Это скрипт-прокси, работающий в отдельном потоке браузера. Он перехватывает сетевые запросы, управляет кэшем и обрабатывает Push-уведомления — даже когда вкладка закрыта.
2. **Преимущества PWA:** Можно установить на экран «Домой», работает офлайн (Service Worker + Cache API), отправляет нативные уведомления (Push API), загружается мгновенно — при этом не требует публикации в App Store.

---

## Сравнительная таблица технологий {#сравнение}

| Критерий | Jetpack Compose (Android) | React Native | Flutter | KMM | PWA |
|---|---|---|---|---|---|
| **Язык** | Kotlin | TypeScript/JS | Dart | Kotlin | JS/HTML/CSS |
| **UI-рендеринг** | Нативный (Android) | Нативный (мост) | Собственный движок | Нативный (по платформе) | Браузерный |
| **Переиспользование кода** | ❌ Только Android | ✅ Android + iOS | ✅ Android + iOS | ✅ Логика (Kotlin) | ✅ Везде (браузер) |
| **Производительность** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **Look & Feel** | Точно нативный | Нативный | Одинаковый везде | Точно нативный | Зависит от браузера |
| **Сложность** | Средняя | Средняя | Средняя | Высокая | Низкая |
| **Установка** | Play Store | Play Store / App Store | Play Store / App Store | Play Store / App Store | Браузер / Home Screen |
| **Офлайн-режим** | Нативный (Room) | Нативный (SQLite) | Нативный (sqflite) | Нативный (SQLDelight) | Service Worker + Cache |

---

## Общий вывод {#вывод}

В ходе лабораторной работы №12 были изучены и практически освоены пять принципиально разных подходов к разработке мобильных и веб-приложений:

- **Jetpack Compose** — лучший выбор для чисто Android-приложений, обеспечивает максимальную интеграцию с экосистемой Google и нативную производительность.
- **React Native** — наиболее популярная кроссплатформенная технология благодаря огромной экосистеме npm и знакомому для веб-разработчиков синтаксису React.
- **Flutter** — даёт самый консистентный UI на всех платформах и высокую производительность за счёт собственного движка рендеринга.
- **KMM** — идеальный компромисс для команд, уже использующих Kotlin: переиспользование логики с сохранением 100% нативного UI.
- **PWA** — самый простой способ «мобилизировать» веб-сайт без публикации в магазинах приложений, с поддержкой офлайн-режима и уведомлений.

Каждая технология имеет свою нишу, и выбор зависит от задачи: качество UI, скорость разработки, нужные платформы и имеющаяся экспертиза команды.
