services:
  postgres:
    image: postgres:16
    container_name: lab14-postgres
    environment:
      POSTGRES_DB: lab14_shop
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
